import { v4 as uuidv4 } from "uuid";

/**
 * Utility function to generate a random key (placeholder).
 * @module getRandomKey
 * @returns {null} Always returns null.
 */
export const getRandomKey = () => {
  return uuidv4();
};

